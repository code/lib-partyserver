---
"partyserver": patch
---

Allow `@cloudflare/workers-types` v5 as a peer dependency. PartyServer remains compatible with v4 while projects using Wrangler 4.108 and newer can install the v5 runtime types without a peer dependency conflict.
